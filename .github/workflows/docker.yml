name: Docker Image CI

on: [push]
jobs:

build:
runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v1
            - name: Build the tagged Docker image
              run: docker build . --file config/template/Dockerfile --tag xxx
